-- File generated by the BNF Converter (bnfc 2.9.5).

{-# LANGUAGE CPP #-}
{-# LANGUAGE FlexibleInstances #-}
{-# LANGUAGE LambdaCase #-}
#if __GLASGOW_HASKELL__ <= 708
{-# LANGUAGE OverlappingInstances #-}
#endif

-- | Pretty-printer for Grammatica.

module Grammatica.Print where

import Prelude
  ( ($), (.)
  , Bool(..), (==), (<)
  , Int, Integer, Double, (+), (-), (*)
  , String, (++)
  , ShowS, showChar, showString
  , all, elem, foldr, id, map, null, replicate, shows, span
  )
import Data.Char ( Char, isSpace )
import qualified Grammatica.Abs

-- | The top-level printing method.

printTree :: Print a => a -> String
printTree = render . prt 0

type Doc = [ShowS] -> [ShowS]

doc :: ShowS -> Doc
doc = (:)

render :: Doc -> String
render d = rend 0 False (map ($ "") $ d []) ""
  where
  rend
    :: Int        -- ^ Indentation level.
    -> Bool       -- ^ Pending indentation to be output before next character?
    -> [String]
    -> ShowS
  rend i p = \case
      "["      :ts -> char '[' . rend i False ts
      "("      :ts -> char '(' . rend i False ts
      "{"      :ts -> onNewLine i     p . showChar   '{'  . new (i+1) ts
      "}" : ";":ts -> onNewLine (i-1) p . showString "};" . new (i-1) ts
      "}"      :ts -> onNewLine (i-1) p . showChar   '}'  . new (i-1) ts
      [";"]        -> char ';'
      ";"      :ts -> char ';' . new i ts
      t  : ts@(s:_) | closingOrPunctuation s
                   -> pending . showString t . rend i False ts
      t        :ts -> pending . space t      . rend i False ts
      []           -> id
    where
    -- Output character after pending indentation.
    char :: Char -> ShowS
    char c = pending . showChar c

    -- Output pending indentation.
    pending :: ShowS
    pending = if p then indent i else id

  -- Indentation (spaces) for given indentation level.
  indent :: Int -> ShowS
  indent i = replicateS (2*i) (showChar ' ')

  -- Continue rendering in new line with new indentation.
  new :: Int -> [String] -> ShowS
  new j ts = showChar '\n' . rend j True ts

  -- Make sure we are on a fresh line.
  onNewLine :: Int -> Bool -> ShowS
  onNewLine i p = (if p then id else showChar '\n') . indent i

  -- Separate given string from following text by a space (if needed).
  space :: String -> ShowS
  space t s =
    case (all isSpace t, null spc, null rest) of
      (True , _   , True ) -> []             -- remove trailing space
      (False, _   , True ) -> t              -- remove trailing space
      (False, True, False) -> t ++ ' ' : s   -- add space if none
      _                    -> t ++ s
    where
      (spc, rest) = span isSpace s

  closingOrPunctuation :: String -> Bool
  closingOrPunctuation [c] = c `elem` closerOrPunct
  closingOrPunctuation _   = False

  closerOrPunct :: String
  closerOrPunct = ")],;"

parenth :: Doc -> Doc
parenth ss = doc (showChar '(') . ss . doc (showChar ')')

concatS :: [ShowS] -> ShowS
concatS = foldr (.) id

concatD :: [Doc] -> Doc
concatD = foldr (.) id

replicateS :: Int -> ShowS -> ShowS
replicateS n f = concatS (replicate n f)

-- | The printer class does the job.

class Print a where
  prt :: Int -> a -> Doc

instance {-# OVERLAPPABLE #-} Print a => Print [a] where
  prt i = concatD . map (prt i)

instance Print Char where
  prt _ c = doc (showChar '\'' . mkEsc '\'' c . showChar '\'')

instance Print String where
  prt _ = printString

printString :: String -> Doc
printString s = doc (showChar '"' . concatS (map (mkEsc '"') s) . showChar '"')

mkEsc :: Char -> Char -> ShowS
mkEsc q = \case
  s | s == q -> showChar '\\' . showChar s
  '\\' -> showString "\\\\"
  '\n' -> showString "\\n"
  '\t' -> showString "\\t"
  s -> showChar s

prPrec :: Int -> Int -> Doc -> Doc
prPrec i j = if j < i then parenth else id

instance Print Integer where
  prt _ x = doc (shows x)

instance Print Double where
  prt _ x = doc (shows x)

instance Print Grammatica.Abs.Ident where
  prt _ (Grammatica.Abs.Ident i) = doc $ showString i
instance Print Grammatica.Abs.Program where
  prt i = \case
    Grammatica.Abs.Prog decls -> prPrec i 0 (concatD [prt 0 decls])

instance Print [Grammatica.Abs.Decl] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString "\167"), prt 0 xs]

instance Print Grammatica.Abs.Decl where
  prt i = \case
    Grammatica.Abs.ConstDecl id_ type_ rexpr -> prPrec i 0 (concatD [doc (showString "def"), prt 0 id_, doc (showString ":"), prt 0 type_, doc (showString ":="), prt 0 rexpr])
    Grammatica.Abs.VarDecl id_ type_ rexpr -> prPrec i 0 (concatD [doc (showString "var"), prt 0 id_, doc (showString ":"), prt 0 type_, doc (showString ":="), prt 0 rexpr])
    Grammatica.Abs.FunDecl id_ parameters type_ block -> prPrec i 0 (concatD [doc (showString "def"), prt 0 id_, doc (showString "("), prt 0 parameters, doc (showString ")"), doc (showString ":"), prt 0 type_, prt 0 block])

instance Print [Grammatica.Abs.Parameter] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print Grammatica.Abs.Parameter where
  prt i = \case
    Grammatica.Abs.Param id_ type_ -> prPrec i 0 (concatD [prt 0 id_, doc (showString ":"), prt 0 type_])

instance Print Grammatica.Abs.Type where
  prt i = \case
    Grammatica.Abs.BasType basictype -> prPrec i 0 (concatD [prt 0 basictype])
    Grammatica.Abs.CompType compoundtype -> prPrec i 0 (concatD [prt 0 compoundtype])

instance Print Grammatica.Abs.BasicType where
  prt i = \case
    Grammatica.Abs.BasicType_bool -> prPrec i 0 (concatD [doc (showString "bool")])
    Grammatica.Abs.BasicType_char -> prPrec i 0 (concatD [doc (showString "char")])
    Grammatica.Abs.BasicType_int -> prPrec i 0 (concatD [doc (showString "int")])
    Grammatica.Abs.BasicType_string -> prPrec i 0 (concatD [doc (showString "string")])
    Grammatica.Abs.BasicType_float -> prPrec i 0 (concatD [doc (showString "float")])
    Grammatica.Abs.BasicType_void -> prPrec i 0 (concatD [doc (showString "void")])

instance Print Grammatica.Abs.Boolean where
  prt i = \case
    Grammatica.Abs.Boolean_True -> prPrec i 0 (concatD [doc (showString "True")])
    Grammatica.Abs.Boolean_False -> prPrec i 0 (concatD [doc (showString "False")])

instance Print Grammatica.Abs.CompoundType where
  prt i = \case
    Grammatica.Abs.ArrDef rexpr type_ -> prPrec i 0 (concatD [doc (showString "["), prt 0 rexpr, doc (showString "]"), prt 0 type_])
    Grammatica.Abs.ArrPartialDef type_ -> prPrec i 0 (concatD [doc (showString "["), doc (showString "]"), prt 0 type_])
    Grammatica.Abs.Pointer type_ -> prPrec i 0 (concatD [doc (showString "&"), prt 0 type_])

instance Print Grammatica.Abs.Block where
  prt i = \case
    Grammatica.Abs.BlockDecl decls statements -> prPrec i 0 (concatD [doc (showString "{"), prt 0 decls, prt 0 statements, doc (showString "}")])

instance Print [Grammatica.Abs.Statement] where
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString "\167"), prt 0 xs]

instance Print Grammatica.Abs.Statement where
  prt i = \case
    Grammatica.Abs.Comp block -> prPrec i 0 (concatD [prt 0 block])
    Grammatica.Abs.ProcCall funcall -> prPrec i 0 (concatD [prt 0 funcall])
    Grammatica.Abs.Jmp jumpstatement -> prPrec i 0 (concatD [prt 0 jumpstatement])
    Grammatica.Abs.Iter iterstatement -> prPrec i 0 (concatD [prt 0 iterstatement])
    Grammatica.Abs.Sel selectionstatement -> prPrec i 0 (concatD [prt 0 selectionstatement])
    Grammatica.Abs.Assgn lexpr assignmentop rexpr -> prPrec i 0 (concatD [prt 0 lexpr, prt 0 assignmentop, prt 0 rexpr])
    Grammatica.Abs.LExprStatement lexpr -> prPrec i 0 (concatD [prt 0 lexpr])

instance Print Grammatica.Abs.Assignment_op where
  prt i = \case
    Grammatica.Abs.Assign -> prPrec i 0 (concatD [doc (showString ":=")])
    Grammatica.Abs.AssgnMul -> prPrec i 0 (concatD [doc (showString "*=")])
    Grammatica.Abs.AssgnAdd -> prPrec i 0 (concatD [doc (showString "+=")])
    Grammatica.Abs.AssgnDiv -> prPrec i 0 (concatD [doc (showString "/=")])
    Grammatica.Abs.AssgnSub -> prPrec i 0 (concatD [doc (showString "-=")])
    Grammatica.Abs.AssgnPow -> prPrec i 0 (concatD [doc (showString "^=")])
    Grammatica.Abs.AssgnAnd -> prPrec i 0 (concatD [doc (showString "&=")])
    Grammatica.Abs.AssgnOr -> prPrec i 0 (concatD [doc (showString "|=")])

instance Print Grammatica.Abs.JumpStatement where
  prt i = \case
    Grammatica.Abs.Break -> prPrec i 0 (concatD [doc (showString "break")])
    Grammatica.Abs.Continue -> prPrec i 0 (concatD [doc (showString "continue")])
    Grammatica.Abs.RetExpVoid -> prPrec i 0 (concatD [doc (showString "return")])
    Grammatica.Abs.RetExp rexpr -> prPrec i 0 (concatD [doc (showString "return"), doc (showString "("), prt 0 rexpr, doc (showString ")")])

instance Print Grammatica.Abs.SelectionStatement where
  prt i = \case
    Grammatica.Abs.IfNoElse rexpr block -> prPrec i 0 (concatD [doc (showString "if"), doc (showString "("), prt 0 rexpr, doc (showString ")"), prt 0 block])
    Grammatica.Abs.IfElse rexpr block1 block2 -> prPrec i 0 (concatD [doc (showString "if"), doc (showString "("), prt 0 rexpr, doc (showString ")"), prt 0 block1, doc (showString "else"), prt 0 block2])

instance Print Grammatica.Abs.IterStatement where
  prt i = \case
    Grammatica.Abs.While rexpr block -> prPrec i 0 (concatD [doc (showString "while"), doc (showString "("), prt 0 rexpr, doc (showString ")"), prt 0 block])

instance Print Grammatica.Abs.RExpr where
  prt i = \case
    Grammatica.Abs.Or rexpr1 rexpr2 -> prPrec i 0 (concatD [prt 0 rexpr1, doc (showString "||"), prt 1 rexpr2])
    Grammatica.Abs.And rexpr1 rexpr2 -> prPrec i 1 (concatD [prt 1 rexpr1, doc (showString "&&"), prt 2 rexpr2])
    Grammatica.Abs.Not rexpr -> prPrec i 2 (concatD [doc (showString "!"), prt 3 rexpr])
    Grammatica.Abs.Eq rexpr1 rexpr2 -> prPrec i 5 (concatD [prt 6 rexpr1, doc (showString "=="), prt 6 rexpr2])
    Grammatica.Abs.Neq rexpr1 rexpr2 -> prPrec i 5 (concatD [prt 6 rexpr1, doc (showString "!="), prt 6 rexpr2])
    Grammatica.Abs.Lt rexpr1 rexpr2 -> prPrec i 5 (concatD [prt 6 rexpr1, doc (showString "<"), prt 6 rexpr2])
    Grammatica.Abs.LtE rexpr1 rexpr2 -> prPrec i 5 (concatD [prt 6 rexpr1, doc (showString "<="), prt 6 rexpr2])
    Grammatica.Abs.Gt rexpr1 rexpr2 -> prPrec i 5 (concatD [prt 6 rexpr1, doc (showString ">"), prt 6 rexpr2])
    Grammatica.Abs.GtE rexpr1 rexpr2 -> prPrec i 5 (concatD [prt 6 rexpr1, doc (showString ">="), prt 6 rexpr2])
    Grammatica.Abs.Add rexpr1 rexpr2 -> prPrec i 7 (concatD [prt 7 rexpr1, doc (showString "+"), prt 8 rexpr2])
    Grammatica.Abs.Sub rexpr1 rexpr2 -> prPrec i 7 (concatD [prt 7 rexpr1, doc (showString "-"), prt 8 rexpr2])
    Grammatica.Abs.Mul rexpr1 rexpr2 -> prPrec i 8 (concatD [prt 8 rexpr1, doc (showString "*"), prt 9 rexpr2])
    Grammatica.Abs.Div rexpr1 rexpr2 -> prPrec i 8 (concatD [prt 8 rexpr1, doc (showString "/"), prt 9 rexpr2])
    Grammatica.Abs.Mod rexpr1 rexpr2 -> prPrec i 8 (concatD [prt 8 rexpr1, doc (showString "%"), prt 9 rexpr2])
    Grammatica.Abs.Pow rexpr1 rexpr2 -> prPrec i 9 (concatD [prt 10 rexpr1, doc (showString "**"), prt 9 rexpr2])
    Grammatica.Abs.Neg rexpr -> prPrec i 11 (concatD [doc (showString "-"), prt 12 rexpr])
    Grammatica.Abs.Ref lexpr -> prPrec i 11 (concatD [doc (showString "&"), prt 0 lexpr])
    Grammatica.Abs.Increment incr -> prPrec i 11 (concatD [prt 0 incr])
    Grammatica.Abs.Lexpr lexpr -> prPrec i 11 (concatD [prt 0 lexpr])
    Grammatica.Abs.FCall funcall -> prPrec i 12 (concatD [prt 0 funcall])
    Grammatica.Abs.Int n -> prPrec i 13 (concatD [prt 0 n])
    Grammatica.Abs.Char c -> prPrec i 13 (concatD [prt 0 c])
    Grammatica.Abs.String str -> prPrec i 13 (concatD [printString str])
    Grammatica.Abs.Float d -> prPrec i 13 (concatD [prt 0 d])
    Grammatica.Abs.Bool boolean -> prPrec i 13 (concatD [prt 0 boolean])
    Grammatica.Abs.Array rexprs -> prPrec i 14 (concatD [doc (showString "["), prt 13 rexprs, doc (showString "]")])

instance Print Grammatica.Abs.Incr where
  prt i = \case
    Grammatica.Abs.PreInc blexpr -> prPrec i 0 (concatD [doc (showString "++"), prt 0 blexpr])
    Grammatica.Abs.PreDecr blexpr -> prPrec i 0 (concatD [doc (showString "--"), prt 0 blexpr])
    Grammatica.Abs.PostInc blexpr -> prPrec i 0 (concatD [prt 0 blexpr, doc (showString "++")])
    Grammatica.Abs.PostDecr blexpr -> prPrec i 0 (concatD [prt 0 blexpr, doc (showString "--")])

instance Print [Grammatica.Abs.RExpr] where
  prt 13 [] = concatD []
  prt 13 [x] = concatD [prt 13 x]
  prt 13 (x:xs) = concatD [prt 13 x, doc (showString ","), prt 13 xs]
  prt _ [] = concatD []
  prt _ [x] = concatD [prt 0 x]
  prt _ (x:xs) = concatD [prt 0 x, doc (showString ","), prt 0 xs]

instance Print Grammatica.Abs.LExpr where
  prt i = \case
    Grammatica.Abs.Deref rexpr -> prPrec i 0 (concatD [doc (showString "$"), prt 0 rexpr])
    Grammatica.Abs.BasLExpr blexpr -> prPrec i 0 (concatD [prt 0 blexpr])

instance Print Grammatica.Abs.BLExpr where
  prt i = \case
    Grammatica.Abs.ArrayEl blexpr rexpr -> prPrec i 0 (concatD [prt 0 blexpr, doc (showString "["), prt 0 rexpr, doc (showString "]")])
    Grammatica.Abs.Id id_ -> prPrec i 0 (concatD [prt 0 id_])

instance Print Grammatica.Abs.FunCall where
  prt i = \case
    Grammatica.Abs.Call id_ rexprs -> prPrec i 0 (concatD [prt 0 id_, doc (showString "("), prt 0 rexprs, doc (showString ")")])
